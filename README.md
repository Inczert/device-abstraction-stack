# DAS [device-abstraction-stack]
DAS, Device Abstraction Stack, is a layered embedded software framework that separates architecture, MCU, board, and peripheral support behind a clean C API. It is designed to replace bulky vendor HALs and code-generation workflows with explicit, maintainable configuration while still allowing low-level control when needed.
